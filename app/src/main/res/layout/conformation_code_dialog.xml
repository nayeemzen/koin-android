<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="20dp">

    <TextView
        android:id="@+id/confirmation_code_dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="Show the code below to the merchant if you entered the amount of transaction"
        android:textAlignment="center"
        android:textSize="15sp"/>

    <ImageView
        android:id="@+id/confirmation_qr_code"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/confirmation_code_dialog_title"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"/>

    <mehdi.sakout.fancybuttons.FancyButton
        android:id="@+id/done_button"
        android:layout_width="65dp"
        android:layout_height="35dp"
        android:layout_below="@id/confirmation_qr_code"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"/>
</RelativeLayout>